# VPC
A VPC (Virtual private cloud), works like a private network to isolate the resources in it.


Like a fence around a bunch of resources. It separates the resources from another vpc and all of its resources

