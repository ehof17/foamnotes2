


if not PaidSellerCampaign
Have to turn on global, then specify campaing to test

Currently it only uses 1 config
So global has to be on for campaign to be on
If we want to test campaign
- If campaignID is what we want to test, test
- If campaignID is not what we want to test, we still test since the global config

Need to have GlobalABTestConfig and CampaignLevelABTestConfig
